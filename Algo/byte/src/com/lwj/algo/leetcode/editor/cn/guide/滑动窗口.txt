1.长度最小的子数组：快慢指针，先用快指针向右遍历到达i位置满足target条件的位置，然后慢指针向右遍历到达j位置不满足target时，i-j+1就是以i为右端点下的最长子数组长度
2.乘积小于 K 的子数组：
3.无重复字符的最长子串：定义一个hashMap存放已出现元素的次数，快指针每遍历一次，将当前遍历元素在map中的计数加一，判断这次累加是否有效（统计map中当前元素
出现的总次数是否大于一，如果大于一的话表示有重复，从慢指针遍历并将遍历的值在map中的次数依次减一，直至快指针所指元素的出现次数等于一）,比较更新最大值

2958
2730
2779
1004
2962
2302
1658
76